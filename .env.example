# Resend API key — required for /api/contact (contact form emails).
# Create at https://resend.com/api-keys
# Local: copy to .env.local (gitignored). Vercel: Project → Settings → Environment Variables.
RESEND_API_KEY=
